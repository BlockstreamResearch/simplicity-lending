use crate::checks::ensure_input_script_hash;

fn script_auth_check(input_script_index: u32) {
    ensure_input_script_hash(input_script_index, param::SCRIPT_HASH);
}

fn main() {
    script_auth_check(witness::INPUT_SCRIPT_INDEX);
}
