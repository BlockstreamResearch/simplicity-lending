# Build output
target/

# IDE/editors (optional but common minimal)
**/.DS_Store
.idea/
.vscode/
.cache
.env

# Debugging data
logs.md
tmp_vendor

configuration.yaml

# Harvester runtime state (collector outpoint moves after every harvest)
crates/harvester/state.json
crates/harvester/state.json.tmp